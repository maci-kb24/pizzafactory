---

const navLinks = [
  { name: 'Почетна', href: '/' },
  { name: 'Мени', href: '/мени/' },
  { name: 'Резервација', href: '/резервација/' },
];
---


<nav>
  <ul class="navigation">
    {navLinks.map((link) => (
      <li class="nav-links"><a href={link.href}>{link.name}</a></li>
    ))}
  </ul>
</nav>

<style>
  /* nav styles */

  .navigation {
    display: flex;
    align-items: center;
    justify-content: space-around;
    transition: background 0.3s ease, box-shadow 0.3s ease;
    background-color: transparent;
  }

  .nav-links {
    width: 100%;
    display: none;
    margin: 0;
  }

  .nav-links a {
    display: block;
    text-align: center;
    padding: 10px 0;
    text-decoration: none;
    font-size: 1rem;
    font-weight: bold;
    text-transform: uppercase;
    color: #fff;
  }

  .nav-links a:hover,
  .nav-links a:focus {
    background-color: #B1C3B8;
    color: #000;
  }

  :has(.menu[aria-expanded="true"]) .nav-links {
    display: unset;
  }

  @media screen and (min-width: 636px) {
    .nav-links {
      display: block;
      position: static;
      width: auto;
      background: none;
    }

    .nav-links a {
      display: inline-block;
      padding: 15px 20px;
    }

    .navigation {
      display: none;
    }
  }
  .navigation {
    list-style: none;
    display: flex;
    gap: 1rem;
    margin: 0;
    padding: 0;
  }

  .navigation a:hover {
    text-decoration: underline;
  }
</style>
